---
title: Bump TheRock tarball to ROCm 7.14.0rc1
date: 2026-07-01
branch: feat/testrunner-kpack-fix
pr: TBD
---

## Summary

Update the default TheRock nightly tarball URL from `7.14.0rc0` to `7.14.0rc1`
(build #127, rockrel run 28472386544, released 2026-07-01).

## Changes

- `Makefile`: `ROCM_VERSION` → `7.14.0rc1`, `ROCM_TARBALL_URL` updated
- `dev.env`: same version + URL bump, comment updated with build provenance

## Validation

Tested on `smc300x-ccs-aus-gpuf268.prov.aus.ccs.cpe.ice.amd.com` (8× MI350P,
gfx950) with the AGFHC+RVS testrunner image built from RC1 tarball:

| Test | Result |
|------|--------|
| minihpl (GPU 0, agfhc `-t minihpl`) | ✅ PASS (2:06) |
| amd-smi GPU enumeration (8 GPUs) | ✅ |
| RVS test suite load (11 suites) | ✅ |

minihpl PASS confirms the `.kpack` fix from PR #1426 is intact in RC1 —
GEMM kernels load correctly via `librocm_kpack.so` + `blas_lib_gfx950.kpack`.

## Risk

Low — tarball URL format unchanged, only version suffix bumped. The install
script (`docker/install-rocm-tarball.sh`) and prune logic are unmodified.
