# Validation Results: GPUOP-723 — Final re-run after refreshing `assets/gpuagent_ual.bin.gz`

- **Run date:** 2026-05-11T16:55:00 PDT
- **Test plan:** `docs/design/test-plans/GPUOP-723-test-plan.md`
- **Execution mode:** Manual e2e on local build host + `${TEST_HOST_NON_IFOE}` (non-IFOE rig)
- **Asset refresh:** `assets/gpuagent_ual.bin.gz` regenerated via `UAL_REMOTE_SERVER=sw-dev3.pensando.io ./scripts/update_ual_assets.sh 1.127.0-116` — replaces the previous (May 7) mock-path-downloaded binary with the real UAL gpuagent fetched from the non-mock path (the change `ed237b0a1` made to the script). This was the gap raised after the v15:00 results: the asset file in repo was stale relative to the script change, so the prior PASSes were against whatever happened to be in the asset (which was the older mock build).
- **Verdict:** **PARTIAL** (all executed tests PASS — same as v15:00; now backed by the real UAL gpuagent binary)

## Summary

| Counter | Value |
|---|---|
| Total | 11 |
| Passed | 8 |
| Failed | 0 |
| Skipped | 3 |
| Pass rate | 8/8 = **100%** (excluding skipped) |

## Per-test-case results (final)

| # | Test Case | Pri | Result | Notes |
|---|-----------|-----|--------|-------|
| 1 | container-build-end-to-end | P0 | **PASS** | Built with real UAL gpuagent (ts 2026-05-11 16:52). Image size unchanged (~922 MB). |
| 2 | ifoe-metrics-scraped-real-hw | P0 | **SKIP** | Need IFOE-capable rig — the only blocker remaining for full Story closure. |
| 3 | non-ifoe-host-graceful-noop | P0 | **PASS** | With **REAL** UAL gpuagent on non-IFOE host: `-monitor-ifoe=true` cmdline ✓; "IFOE disabled" log fires exactly once at `gpuagent_ifoe.go:271` ✓; zero `amd_ifoe_*` series ✓; **`amd_gpu_*` series flowing** (real 1-GPU host produces 1 series — fewer than the prior mock's 10, which is expected on real hardware) ✓; gpuagent stays alive ✓; no panics ✓. |
| 4 | enable-ifoe-false-disables | P1 | **PASS** | `-monitor-ifoe=false` reaches binary, IFOE codepath never invoked, GPU monitoring intact. |
| 5 | enable-ifoe-invalid-value | P1 | **PASS** | WARN log + default true; gate fires once. |
| 6 | mock-path-removed-universal | P1 | **PASS** | **Now PASS** (was SKIP) — script ran successfully from this build host: `UAL_REMOTE_SERVER=sw-dev3.pensando.io ./scripts/update_ual_assets.sh 1.127.0-116` fetched from `sw-dev3.pensando.io:/vol/builds/hourly/1.127.0-116/rudra-bundle/internal-artifacts/nodemgmt/gpuagent_1.127.0-116.tar.gz` (the **non-mock** path — confirms commit `ed237b0a1` change works end-to-end). Output: `assets/gpuagent_ual.bin.gz` (9,227,954 bytes, 2026-05-11 16:52) — different binary from the prior mock-based 9,196,082-byte file. |
| 7 | deb-ual-still-builds | P1 | **SKIP** | `make libcopy-assets-UBUNTU22` requires `registry.test.pensando.io:5000` (internal registry, blocked from this build host). Environmental. |
| 8 | rpm-ual-still-builds | P1 | **SKIP** | Same. |
| 9 | libamd-smi-26.3.0-linkage | P1 | **PASS** | Real UAL gpuagent has `/home/amd/lib/libamd_smi.so.26.3.0` mapped into its address space (per `/proc/<pid>/maps`). No symbol errors. F2 (libamd_smi 26.3.0 ABI compat with the real UAL gpuagent) confirmed. |
| 10 | runtime-libs-installed | P1 | **PASS** | Unchanged. |
| 11 | gpuagent-grpc-failure-recovery | P1 | **PASS** | Unchanged — exporter retries with backoff after gpuagent kill, no panic, /metrics 200. |

## Findings status (final)

- **F1** (gpuagent_static vs gpuagent_ual bundling): **FIXED** in commit `79336c35d` — `build_prep_docker.sh` UAL=1 default. Verified end-to-end with real UAL gpuagent.
- **F2** (libamd_smi 26.3.0 ABI compat): **VERIFIED** by TC09 PASS. Real UAL gpuagent runs cleanly with libamd_smi 26.3.0.
- **F3** (dead root entrypoint.sh): **FIXED** in commit `79336c35d`.
- **F4** (internal registry not reachable): environmental — blocks TC07/TC08 only.
- **F5 (newly raised + addressed in this run)**: `assets/gpuagent_ual.bin.gz` was stale (May 7, before the GPUOP-737 script change). Refreshed in this run via the updated script. Asset now reflects real gpuagent at version 1.127.0-116.

## Verdict

**Story-test verdict: PARTIAL.** All 8 executed test cases PASS, including TC03 backed by the real UAL gpuagent. The 3 SKIP cases are infra-blocked (TC02 needs IFOE-capable rig; TC07/TC08 need internal-registry access — environmental, not GPUOP-723 bugs).

GPUOP-723 is now functionally complete on the non-IFOE side. The single remaining gate for full Story closure is TC02 on a real IFOE rig — that requires hardware unavailable in this session.
